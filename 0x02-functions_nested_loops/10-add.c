#include "main.h"
/**
 * add -> adds two integers
 * @num1: a parameter
 * @num2: a parameter
 * Return: Returns calAdd
 */
int add(int num1, int num2)

{
	int calAdd = num1 + num2;

	return (calAdd);
}
