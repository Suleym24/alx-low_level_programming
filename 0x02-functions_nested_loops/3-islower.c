#include "main.h"
/**
 * _islower -> checks if character is lowerecase
 * @c: a character argument
 * Return: returns 1 and 0 depending in condition
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
